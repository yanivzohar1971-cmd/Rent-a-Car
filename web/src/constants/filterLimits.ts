/**
 * Filter limits constants
 * 
 * Single source of truth for filter range limits across the application.
 * This ensures consistency between dialogs, sanitization, and validation logic.
 */

export const MIN_KM = 0;
export const MAX_KM = 500_000;
